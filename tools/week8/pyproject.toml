[project]
name = "week8-finalization-tools"
version = "0.1.0"
requires-python = ">=3.10,<3.11"
dependencies = [
  "pytest==8.4.1",
  "vulture==2.16",
]

[tool.pytest.ini_options]
testpaths = ["tests"]

