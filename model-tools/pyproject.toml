[project]
name = "content-retrieval-model-tools"
version = "0.1.0"
requires-python = ">=3.10,<3.11"
dependencies = [
    "numpy>=2.0",
    "sentence-transformers>=5,<6",
]

[dependency-groups]
dev = [
    "pytest>=8.3,<9",
]
