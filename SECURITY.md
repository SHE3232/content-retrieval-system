# 安全策略

## 支持范围

当前维护 `1.0.x` 系列。默认应用仅面向本机单用户场景，后端必须绑定回环地址；它不包含面向公网部署所需的身份认证、授权或多租户隔离。

## 报告安全漏洞

请不要通过公开 Issue、讨论区、日志附件或演示材料披露未修复漏洞、凭据、用户文件或可直接利用的细节。公开仓库建立后，优先使用 GitHub 的私密安全漏洞报告功能；在此之前，请通过课程既有的私密联系渠道联系项目维护者。

报告建议包含受影响版本、复现条件、影响范围、最小复现步骤和可行的缓解建议。维护者会确认收到报告、核实影响、准备修复与回归测试，并在修复可用后协调披露。

## 不在默认安全承诺内的场景

- 把本地 API 暴露到其他主机或互联网；
- 绕过模型、数据或第三方材料的许可证限制；
- 使用未经校验的模型清单、Tika JAR、运行时或发布归档；
- 在已被篡改的操作系统或依赖环境中运行。
